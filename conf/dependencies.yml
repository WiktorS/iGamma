# Application dependencies

require:
    - play
    - play -> coffee 1.0
    - play -> less 0.9.1
    # JNA is used to access library functions
    - net.java.dev.jna -> jna 3.5.2
    # User passwords are secured with BCrypt
    - org.mindrot -> jbcrypt 0.3m
    # Users data are stored in SQLite database
    - org.xerial -> sqlite-jdbc 3.7.2
